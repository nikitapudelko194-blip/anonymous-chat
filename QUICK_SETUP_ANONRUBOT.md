# 🚀 Быстрые инструкции для запуска

## Что имеется:

✅ **Рекомендуемая версия** анонимного чата  
✅ 👍👎 Система оценок  
✅ 💎 VIP подписка с тремя тарифами  
✅ 👤 Поиск по полу  
✅ 💬 Выбор интересов  
✅ 🐛 Улучшенная система жалоб  

---

## Шаг 1: Установка

### Вариант A: Если у вас уже есть полные права к репо

```bash
# 1. Пулл новых изменений
git pull origin main

# 2. Установить/обновить зависимости
pip install -r requirements.txt

# 3. Настройка .env
cat > .env << EOF
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
EOF

# 4. Запуск
python bot/main.py
```

### Вариант B: Новичком

1. **Откройте Terminal/PowerShell**

2. **Клонируйте репо**
```bash
git clone https://github.com/nikitapudelko194-blip/anonymous-chat.git
cd anonymous-chat
```

3. **Установите Python 3.8+ (если не есть)**

4. **Установите зависимости**
```bash
pip install -r requirements.txt
```

5. **Создайте `.env` файл в корне**

   Windows:
   ```
   BOT_TOKEN=your_token_here
   ```

   Linux/Mac:
   ```bash
   echo "BOT_TOKEN=your_token_here" > .env
   ```

6. **Запустите бот**
```bash
python bot/main.py
```

---

## Шаг 2: Получите BOT_TOKEN

1. Откройте **@BotFather** в Telegram
2. Нажмите `/start`
3. Нажмите `/newbot`
4. Назовите бота (example: `MyAnonBot`)
5. Назовите юзернейм (example: `my_anon_bot`)
6. Готово! Вы наф токен

---

## Команды бота

### для меню ботфатера:

```
start - Начать работу с ботом
search - Найти собеседника
next - Следующий собеседник
stop - Завершить чат
interests - Выбрать интересы
settings - Настройки
rules - Правила
help - Помощь
vip - VIP планы
```

---

## ⚠️ Ошибки и решения

### Ошибка: `ModuleNotFoundError: No module named 'aiogram'`

Решение:
```bash
pip install aiogram python-dotenv
```

### Ошибка: `BOT_TOKEN не установлен`

Решение:
1. Получите токен у @BotFather
2. Создайте `.env` файл
3. Добавьте токен:
   ```
   BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
   ```

### Ошибка: `ConnectionError`

Решение:
- Проверьте интернет соединение
- Проверьте токен
- Перезапустите бот

---

## 📋 Импортантные файлы

```
anonymous-chat/
├─ bot/
│  └─ main.py          🔥 Основной файл бота
├─ .env               🔒 Кадуц за TOKEN
├─ config.py          ⚙️ Конфигурация
├─ requirements.txt    депенди
├─ chat_bot.db        💾 База данных (SQLite)
├─ ANON_RUBOT_UPDATES.md  📚 Полная документация
└─ README.md          ℹ️ Общие сведения
```

---

## ✨ Новые таблицы в ДБ

### Новые особенности таблицы `users`:
- `interests` - интересы пользователя
- `positive_votes` - количество положительных оценок
- `negative_votes` - количество отрицательных оценок
- `rating` - рейтинг (в %)
- `is_premium` - VIP статус
- `premium_expires_at` - когда истекает VIP
- `status` - онлайн/оффлайн
- `last_activity` - последняя активность

### Новые таблицы:
- **`votes`** - система оценок (НОВАЯ)
- **`payments`** - система платежей (НОВАЯ)

---

## 🙋 Полная документация

Полную документацию новых функций см. в **ANON_RUBOT_UPDATES.md**

---

## 🙋 Вопросы?

Нажмите **"/help"** в боте для полного списка команд.

Про сноси: @AnonBotAdmin

---

**🌟 Ротово пойс ется!**
