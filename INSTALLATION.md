# 🚀 Установка и запуск

## Пю Основные потребности

- Python 3.9+
- pip
- git

## Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/nikitapudelko194-blip/anonymous-chat.git
cd anonymous-chat
```

## Шаг 2: Создание виртуального окружения

### Windows:
```bash
python -m venv venv
venv\Scripts\activate
```

### macOS/Linux:
```bash
python3 -m venv venv
source venv/bin/activate
```

## Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 4: Настройка переменных окружения

### На Linux/macOS:
```bash
cp .env.example .env
```

### На Windows:
```bash
copy .env.example .env
```

## Шаг 5: Определение BOT_TOKEN

1. Откройте файл `.env` в любом текстовом редакторе
2. Найдите строку `BOT_TOKEN=your_bot_token_here`
3. Вставьте ваш Telegram Bot API Token

### Как получить token:

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Напишите `/newbot`
3. Дофоллов он укажит данные
4. От BotFather придет сообщение с token
5. Копируйте token в `.env` файл

## Шаг 6: Запуск бота

```bash
python -m bot.main
```

Если бот успешно запустился, вы увидите:
```
🚀 Бот запущен
```

## Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по наименованию (которое вы указали в BotFather)
3. Напишите `/start`
4. Начните регистрацию

## Файлы данных

- На экране сохраняется файл `chat_bot.db` (SQLite)
- Его не нужно заливать в git (есть в `.gitignore`)

## Проблемы при инсталлации

### Ошибка: `ModuleNotFoundError: No module named 'aiogram'`

Тогда мас активировали виртуальныю среду?

```bash
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate  # Windows
```

### Ошибка: `BOT_TOKEN not found`

Проверьте, что `.env` файл есть и он исправлен.

## Дальнейшая разработка

Комплетная документация в [README.md](./README.md)
