# 🚀 Как правильно запустить бота

## ❌ НЕПРАВИЛЬНО (из папки bot/):
```bash
cd bot
python main.py
```
**Ошибка:** `ModuleNotFoundError: No module named 'bot'`

---

## ✅ ПРАВИЛЬНО (из корневой папки проекта):

### Вариант 1: Через Python модуль
```bash
# Убедитесь что находитесь в корне проекта
cd path/to/anonymous-chat-main

# Запустите бота
python -m bot.main
```

### Вариант 2: Напрямую через Python
```bash
# Из корня проекта
cd path/to/anonymous-chat-main

# Запустите
python bot/main.py
```

---

## 📋 Структура папок (ВАЖНО!)

```
anonymous-chat-main/          ← ВЫ ДОЛЖНЫ БЫТЬ ЗДЕСЬ!
├── bot/
│   ├── __init__.py           ← Это делает bot пакетом
│   ├── main.py               ← Точка входа
│   ├── config.py
│   ├── database/
│   ├── handlers/
│   ├── keyboards/
│   ├── states/
│   ├── utils/
│   └── ...
├── .env                       ← Файл с BOT_TOKEN
├── requirements.txt
└── README.md
```

---

## 🔧 Полная инструкция (с нуля)

### 1. Откройте Windows PowerShell/CMD в корне папки проекта:
```bash
cd C:\Users\User\Downloads\anonymous-chat-main
```

### 2. Активируйте виртуальное окружение:
```bash
# PowerShell:
.venv\Scripts\Activate.ps1

# ИЛИ CMD:
.venv\Scripts\activate.bat
```

### 3. Установите зависимости (если еще не установлены):
```bash
pip install -r requirements.txt
```

### 4. Создайте .env файл (если его еще нет):
```bash
echo BOT_TOKEN=your_token_here > .env
```

### 5. **ЗАПУСТИТЕ БОТА ИЗ КОРНЯ:**
```bash
python -m bot.main
```

**ИЛИ:**
```bash
python bot/main.py
```

---

## ✅ Если всё работает:

Вы должны увидеть:
```
2025-12-28 15:54:31,234 - __main__ - INFO - 🚀 Инициализация бота 'Анонимный Чат'...
2025-12-28 15:54:31,235 - __main__ - INFO - 📁 Инициализация базы данных...
2025-12-28 15:54:31,236 - __main__ - INFO - ✅ База данных инициализирована успешно
2025-12-28 15:54:31,237 - __main__ - INFO - 🤖 Создание экземпляра бота...
2025-12-28 15:54:31,238 - __main__ - INFO - ✅ Бот создан успешно
2025-12-28 15:54:31,239 - __main__ - INFO - 📡 Создание диспетчера...
2025-12-28 15:54:31,240 - __main__ - INFO - 🔌 Подключение роутеров...
2025-12-28 15:54:31,241 - __main__ - INFO -   ✓ Все обработчики подключены
2025-12-28 15:54:31,242 - __main__ - INFO - 🚀 БОТ ЗАПУЩЕН И ГОТОВ К РАБОТЕ!
2025-12-28 15:54:31,243 - __main__ - INFO - 💬 Ожидание входящих сообщений...
```

---

## 🔑 Ключевые моменты:

1. **`bot/__init__.py` существует** - это нужно для импортов
2. **Импорты используют относительные пути** - `from config import BOT_TOKEN` вместо `from bot.config import BOT_TOKEN`
3. **sys.path.insert(0, ...)** в main.py добавляет корневую папку в поиск модулей
4. **ВСЕГДА запускайте из корня проекта**, а не из папки bot/

---

## 🆘 Если всё ещё не работает:

```bash
# Проверьте что находитесь в корне:
pwd
# Должно вывести: C:\Users\User\Downloads\anonymous-chat-main

# Проверьте структуру:
ls -la bot/
# Должны быть файлы: __init__.py, main.py, config.py, etc.

# Проверьте .env:
type .env
# Должен содержать: BOT_TOKEN=xxx

# Запустите с максимальным логированием:
python -u -m bot.main
```

Далее скопируйте ошибку и поделитесь ей! 🔧
